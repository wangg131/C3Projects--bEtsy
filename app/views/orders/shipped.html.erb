<%= render :partial => 'orders',
           :locals => { :merchant => @merchant,
                        :order_items => @order_items,
                        :orders => @orders
                       } %>
