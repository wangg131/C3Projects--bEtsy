<%= render :partial => 'orders',
           :locals => { :merchant => @merchant,
                        :order_items => @order_items,
                        :orders => @orders,
                        :shipped_revenue => @shipped_revenue,
                        :unshipped_revenue => @unshipped_revenue

                        } %>
