<h3><%= @unit_total %> <%= @unit_total == 1 ? "Item" : "Items" %> in Your Cart</h3>

<% if @order_items.count == 0 %>
  <p class="text-center">
    There are no items in your shopping cart.  Please <%= link_to "go back", root_path %> and add some items to your cart.
  </p>
<% else %>
  <div class="row">
    <table class="table">
      <tr>
        <th></th>
        <th></th>
        <th></th>
        <th>Name</th>
        <th>Total</th>
      </tr>
      <% @order_items.each do |item| %>
        <tr>
          <td></td>
          <td></td>
          <td><%= image_tag(item.product.photo_url, class: "cart-thumbnail") %></td>

          <td><%= link_to "#{item.product.name}", merchant_product_path(merchant_id: item.product.merchant_id, id: item.product.id) %></td>

          <td><%= number_to_currency(item.revenue) %></td>
        </tr>
      <% end %>
      <tr>
        <td></td>
        <td></td>
        <td></td>
        <td><strong>Total before shipping: </strong></td>
        <td><strong><%= number_to_currency(@order_total) %></strong></td>
      </tr>
    </table>
  </div>
<% end %>


<h3>USPS Delivery 1, $ Rate, $ Total Price <%= link_to "Select", edit_order_path(current_order.id), class: "btn btn-primary" %></h3>
<h3>USPS Delivery 2, $ Rate, $ Total Price <%= link_to "Select", edit_order_path(current_order.id), class: "btn btn-primary" %></h3>
<h3>UPS Delivery 1, $ Rate, $ Total Price <%= link_to "Select", edit_order_path(current_order.id), class: "btn btn-primary" %></h3>

<%= @shipment_response %>
