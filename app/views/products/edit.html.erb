<h1>Edit Product</h1>

<%= form_for @product, url: merchant_product_path(params[:merchant_id], params[:id]) do |f| %>
  <fieldset>
    <legend>Product Details</legend>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_area :description %>

      <%= f.label :price %>
      <%= f.number_field :price %>

      <%= f.label :stock %>
      <%= f.number_field :stock %>

      <%= f.label :active %>
      <%= f.select :active, [['Active', true], ['Inactive', false]] %>

      <%= f.label :photo_url %>
      <%= f.text_field :photo_url %>

      <%= f.hidden_field :merchant_id, :value => params[:merchant_id] %>
  </fieldset>
  <%= f.submit %>
<% end %>
